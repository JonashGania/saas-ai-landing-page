import React from "react";

const Hero = () => {
  return (
    <section className="h-screen w-full hero bg-cover bg-center bg-no-repeat">
      <div></div>
    </section>
  );
};

export default Hero;
